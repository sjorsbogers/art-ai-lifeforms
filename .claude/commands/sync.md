Update the CHANGELOG.md and lifeforms/01-pin-grid/CHANGELOG.md with the provided message under [Unreleased], then commit all changes and push using: ./sync.sh "$ARGUMENTS"

If no message is provided, ask for one before proceeding.
